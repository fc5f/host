<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .bg-primary { background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 50%, #60a5fa 100%); }
        .btn-primary { background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%); }
        .btn-primary:hover { background: linear-gradient(135deg, #1d4ed8 0%, #1e40af 100%); }
    </style>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full">
        <div class="bg-white rounded-2xl shadow-2xl p-8">
            <!-- الهيدر -->
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-16 h-16 bg-blue-500 rounded-full mb-4">
                    <i class="fas fa-robot text-2xl text-white"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800">تسجيل الدخول</h1>
                <p class="text-gray-600 mt-2">أدخل كود التحقق للدخول إلى لوحة التحكم</p>
            </div>

            <!-- رسائل الخطأ -->
            <% if (error) { %>
                <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
                    <div class="flex items-center">
                        <i class="fas fa-exclamation-triangle text-red-500 ml-3 text-lg"></i>
                        <div>
                            <h3 class="font-semibold text-red-800">خطأ في التسجيل</h3>
                            <p class="text-red-700 text-sm mt-1"><%= error %></p>
                        </div>
                    </div>
                </div>
            <% } %>

            <!-- نموذج تسجيل الدخول -->
            <form method="POST" action="/login" class="space-y-6">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-3">
                        <i class="fas fa-key text-blue-500 ml-2"></i>
                        كود التحقق:
                    </label>
                    <div class="relative">
                        <input type="text" name="code" required 
                               placeholder="أدخل كود التحقق المكون من 6 أحرف"
                               class="w-full px-4 py-4 border-2 border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200 text-lg text-center font-mono tracking-widest"
                               maxlength="6"
                               style="letter-spacing: 0.2em; font-size: 1.2rem;"
                               value="<%= typeof code !== 'undefined' ? code : '' %>">
                        <div class="absolute left-3 top-1/2 transform -translate-y-1/2">
                            <i class="fas fa-shield-alt text-gray-400"></i>
                        </div>
                    </div>
                    <p class="text-gray-500 text-sm mt-2 flex items-center justify-center">
                        <i class="fas fa-info-circle ml-1 text-blue-500"></i>
                        احصل على كود التحقق من سيرفر الديسكورد باستخدام الأمر <code class="bg-gray-100 px-2 py-1 rounded mx-1">/تحقق</code>
                    </p>
                </div>

                <button type="submit" 
                        class="w-full btn-primary text-white py-4 rounded-xl font-semibold text-lg hover:shadow-lg transition duration-200 transform hover:scale-[1.02]">
                    <i class="fas fa-sign-in-alt ml-3"></i>
                    تسجيل الدخول
                </button>
            </form>

            <!-- معلومات إضافية -->
            <div class="mt-8 space-y-4">
                <div class="bg-blue-50 rounded-lg border border-blue-200 p-4">
                    <div class="flex items-start">
                        <i class="fas fa-robot text-blue-500 mt-1 ml-3 text-lg"></i>
                        <div>
                            <h3 class="font-semibold text-blue-800">كيفية الحصول على كود التحقق:</h3>
                            <ol class="text-blue-700 text-sm mt-2 space-y-2">
                                <li class="flex items-center">
                                    <span class="bg-blue-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs ml-2">1</span>
                                    اذهب إلى سيرفر الديسكورد
                                </li>
                                <li class="flex items-center">
                                    <span class="bg-blue-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs ml-2">2</span>
                                    اكتب الأمر: <code class="bg-blue-100 px-2 py-1 rounded text-blue-800 font-mono">/تحقق</code>
                                </li>
                                <li class="flex items-center">
                                    <span class="bg-blue-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs ml-2">3</span>
                                    انسخ الكود من الرسالة الخاصة وأدخله هنا
                                </li>
                            </ol>
                        </div>
                    </div>
                </div>
            </div>

            <!-- رابط العودة -->
            <div class="text-center mt-6">
                <a href="/" class="text-blue-600 hover:text-blue-800 transition duration-200 font-semibold">
                    <i class="fas fa-arrow-right ml-2"></i>
                    العودة للصفحة الرئيسية
                </a>
            </div>
        </div>
    </div>

    <script>
        // التركيز على حقل الإدخال وتحديد النص
        document.addEventListener('DOMContentLoaded', function() {
            const codeInput = document.querySelector('input[name="code"]');
            if (codeInput) {
                codeInput.focus();
                codeInput.select();
            }
        });

        // تحويل النص إلى أحرف كبيرة تلقائياً
        document.querySelector('input[name="code"]').addEventListener('input', function(e) {
            this.value = this.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
        });

        // إظهار تحميل عند الإرسال
        document.querySelector('form').addEventListener('submit', function() {
            const btn = this.querySelector('button[type="submit"]');
            btn.innerHTML = '<i class="fas fa-spinner fa-spin ml-3"></i> جاري التحقق...';
            btn.disabled = true;
        });
    </script>
</body>
</html>
